# mini_project_0313
Traffic Monitoring and Anomaly Detection

* 서버실행 명령어
  frontend : npm install → npm run dev
  
  backend : pip install -r requirements.txt

  .env 설정 후 프로젝트 폴더에 추가

  DB초기화 후 python app.py 실행
